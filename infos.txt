git checkout develop
git pull origin develop

git merge feature/add-alb-backend
git push origin develop

--rollback
git revert -m 1 <hash-do-commit-de-merge>
git push origin develop

commit-v1.1



resource "aws_lb_target_group_attachment" "nlb_to_alb_attachment" {
  target_group_arn = "arn:aws:elasticloadbalancing:us-east-1:123456789012:targetgroup/existing-tg-name/abcdef123456"
  target_id        = aws_lb.ALBBanckend.arn
  port             = 8081
}